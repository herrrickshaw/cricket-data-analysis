# cricket-data-analysis

Extracted from stray `fetch_and_analyze_cricket_data.R` copies found in `global-stock-screener` and `global-market-data` during a GitHub portfolio declutter — unrelated to either repo's stock-market purpose. History preserved from `global-stock-screener` (identical content also existed in `global-market-data`).
